/**
 * Adds a and b.
 * @constructor
 * @param {number} a - A number.
 * @param {number} b - Another number to add.
 */
function add(a: number, b: number): number {
  return a + b
}

export default add;